# iTestAgent 开发计划安排文档

## 0. 文档说明

本计划基于全部文档（用户故事与验收标准、架构设计、技术选型、数据流全链路、AI Native 开发手册、开发避坑手册）编制，给出**分阶段、可执行、可验收**的开发排期与任务拆解。

配套文档：

```
iTestAgent 全量用户故事与验收标准规格书
iTestAgent 架构设计文档
iTestAgent 技术选型文档
iTestAgent 数据流全链路技术说明文档
iTestAgent AI Native 开发理念与实战技巧手册
iTestAgent 开发避坑与关键注意点手册
```

排期单位约定：

人力：1 名独立开发者（全栈，AI Native 全程） 相对周 W 按单人全职折算，兼职按投入比例线性放大 里程碑以可验收结果为准，不以代码量为准

## 1. 总体策略

核心原则（来自避坑手册与可行性分析）：

```
1. 先 Spike 后功能：先验证真机链路与元素可定位性，再堆能力
2. 去风险 MVP：人在环路记录器 + 稳健性能趋势工具
3. 研究级能力后置：自动链路推断/自动断言/FPS 精确/xctrace 深解析 标 Deferred/实验性
4. 每阶段以 P0 验收为门槛，P1 允许已知限制并声明
5. AI Native 全流程：规格即上下文、小步可验证、真机必实测
```

阶段总览：

```
Phase 0  立项与多 Backend 横评（决定路线）
Phase 1  骨架与环境（CLI/TUI/contracts/Server/doctor/devices/store/config + Simulator spike）
Phase 2  项目分析与 TestPlan（Project Profile + 计划确认）
Phase 3  真机执行核心（构建安装 + XCUITest + DeviceBackend 探索录制）
Phase 4  证据/性能/报告（稳健性能子集 + 三件套报告 + 归因）
Phase 5  打磨与 MVP 验收（Flow 重放/重跑/安全/门禁）
Phase 6  真机全闭环收口（P0 恢复线）
Phase 7  MVP 后增强（可选）
```

## 2. Phase 0：立项与多 Backend 横评（约 W1-W2）

目标：用最小成本验证各层 backend 候选，选出 MVP 最优组合。

核心原则：**不预设单一工具路线。** 每一层都做 2-4 候选横评，用 Phase 0 spike 数据决定 MVP 主力 backend。

任务：

```
T0.1 建仓与工程规范
T0.2 Device backend 横评（最高优先）
T0.3 Performance backend 横评
T0.4 TUI backend 横评
T0.5 Project analyzer 横评
T0.6 技术验证结论
  - monorepo(Bun)、AGENTS.md、schemas/、fixtures/、mock backends 脚手架

  - Spike A: mobile-mcp 路线 — 设备发现、安装/启动 app、UI tree、截图、tap/swipe/type
  - Spike B: Appium/WDA 路线 — WDA 签名安装、Appium session、page source、screenshot、gesture
  - Spike C: iphone-use 路线 — iPhone Mirroring + WebRTC + 视觉 fallback（仅当 A/B 失败时）
  - Spike D: mock backend — fixtures 模拟所有 backend 输出，保障无真机开发和 CI

  - Spike P1: XcodeTraceMCP / @xctrace-analyzer/core — trace export、hitches/hangs、not_exportable
  - Spike P2: instrumentsmcp — 录制+分析+report 一体化（T0.3 发现数据诚信问题，降级为参考）
  - Spike P3: raw xcrun xctrace export — 最小自研 fallback

  - OpenTUI: 验证长日志、Markdown、工具调用卡片、Bun 打包
  - ~~Rezi~~: 已排除（npm rezi 是 2015 CSS 工具，非 TUI 框架）
  - Ink: 验证最小交互版是否足够支撑 MVP

  - XcodeQuery CLI: 验证 JSON 输出是否足够稳定（T0.5 结果：本机不可用，降级为 optional future）
  - XcodeProj + xcodebuild: 传统方案作为对照

  - 输出决策矩阵 + Phase 0 横评报告
  - 确定 MVP 各层 backend 选择
```

里程碑 M0：

```
- device backend 至少一路在真机上跑通（截图+UI tree+launch time）
- performance backend 至少一路能获取 hitches/hangs
- TUI backend 至少一路能跑通交互式 Shell
- 决策矩阵明确：每层选 MVP 主力 + fallback
出口标准：M0 未过则暂停功能开发，先解决真机/性能瓶颈
```

决策矩阵：

| 如果 | 则 |
|---|---|
| mobile-mcp 真机稳定且付费账号可用 | 可补测后作为 mobile-mcp device backend |
| mobile-mcp 被云壳/免费账号限制阻断 | AppiumBackend 做主线（T0.2b 验证免费账号可用） |
| WDA UI tree 质量差 | 研究 iphone-use 视觉 fallback |
| XcodeTraceMCP core 稳定 | 性能模块优先复用 core |
| XcodeTraceMCP core 不适合依赖 | raw-xcrun-minimal fallback |
| OpenTUI 不稳定 | Ink fallback（T0.4 已验证 16/16 通过） |
| XcodeQuery CLI JSON 稳定 | ~~优先作为 Project Profile backend~~ → T0.5 不可用，raw xcodebuild + Tuist/XcodeProj 优先 |

## 3. Phase 1：骨架与环境（约 W3-W5）

目标：跑通 TUI-first 骨架与环境诊断，用户能进入 Agent 并看到真机与 Simulator 设备（ADR-011 同级支持）。

任务（对应 US）：

```
T1.1 CLI 入口 + 版本/配置                     US-1.1, US-18.1/18.2
T1.2 TuiShell 交互式 Shell 骨架                 US-4.1（OpenTUI+SolidJS 默认，ADR-008）
T1.3 Harness 核心接口契约：AgentRuntime/AgentEvent/ToolCall/ToolResult + Backend interfaces + 数据契约  架构 §5/数据流/ADR-010（✅ done）
T1.4 ADR-011 + Simulator 契约扩展：TargetKind, BackendCapabilities, Schema v2  ADR-011（依赖 T1.3）
T1.5 Simulator Spike：simctl lifecycle + Simulator SDK build         （依赖 T1.4）
T1.6 Simulator Spike：Appium/WDA Simulator session + UI/actions      （依赖 T1.5）
T1.7 RunStateMachine：生命周期/错误状态/暂停/恢复/取消  架构 §7/ADR-010（依赖 T1.3）
T1.8 本地 Bun server + SSE Hub + 基础路由          US-4.3（依赖 T1.3）
T1.9 存储骨架(SQLite/Drizzle+目录)                   US-19.1（阻塞 T1.14/T2.1）
T1.10 配置分层(JSONC)+Keychain 接入（含 AI SDK major 锁定）US-18.2/18.3（阻塞 T1.15/T3.16）
T1.11 doctor Physical readiness：Xcode/签名/信任/Appium    US-1.2, US-1.3
T1.12 doctor Simulator readiness：runtime/simctl/SDK/Appium  US-1.2, US-1.3
T1.13 devices 设备发现 + backend healthcheck          US-2.1, US-2.2
T1.14 SessionManager：session 创建/关闭/隔离/超时    US-4.3（依赖 T1.8, T1.7, T1.9）
T1.15 Subprocess controller + mock AgentRuntime      （依赖 T1.14, T1.10）
T1.16 Phase 1 集成测试：骨架与环境联调验证（含 Simulator 骨架）
```

里程碑 M1：

```
- itestagent 进入 TUI；doctor/devices 可用并给引导
- Harness 核心接口契约（AgentRuntime/AgentEvent/ToolCall/ToolResult + Backend interfaces）定义完成
- RunStateMachine 状态迁移实现完成
- 本地 server 管理长任务/事件流 + SessionManager + subprocess controller
- 存储与配置骨架就位
出口标准：US-1.x/2.x/4.1/4.3/18.x 的 P0 全绿
```

## 4. Phase 2：项目分析与 TestPlan（约 W6-W8）

目标：先理解项目再测试；自然语言可编译为可确认的 TestPlan。

任务：

```
T2.1 工程结构解析(XcodeProj + xcodebuild -list/-showBuildSettings)  US-3.1
T2.2 Swift/ObjC tier 1 候选扫描（SwiftSyntax/SourceKit 条件增强）   US-3.1（ADR-026）
T2.3 生成 Project Profile(schema 化)                                US-3.2
T2.4 候选核心链路(证据+置信度)+TUI 勾选确认                          US-3.3
T2.5 Intent 解析(自然语言)                                          US-4.2
T2.6 TestPlan 编译器(过 plan.yaml schema)                          US-5.1
T2.7 计划展示与确认(TUI)+自然语言修改                                US-5.2
T2.8 Phase 2 集成测试：项目分析与 TestPlan 联调验证
```

里程碑 M2：

```
- 启动即分析项目并产出 Project Profile（结构确定性字段可靠）
- 候选链路带证据、用户可确认
- 一句话可生成并确认 TestPlan
出口标准：US-3.x/4.2/5.x 的 P0 全绿；链路推断只做候选（不自动断定）
```

对应文档：架构 5/6、数据流 S1/S2/S3、避坑手册第 5 节。

## 5. Phase 3：真机执行核心（约 W9-W13）

目标：真机与 Simulator 上跑通双路径执行，探索式落地为可重放 Flow（ADR-011 双目标）。

任务：

```
T3.1 App 来源决策 + xcodebuild 构建 + xcbeautify           US-6.1
T3.2 DeviceBackend mock 实现 + target-aware BackendSelector   架构 §5.4/ADR-011
T3.3 PermissionEngine：allow/ask/deny + 记忆规则 + 高风险拦截 US-17.2/ADR-010
T3.4 ContextBuilder：上下文组装（targetKind 感知）             ADR-010
T3.5 BuildDriver: destination union(physical/simulator)+签名   US-6.2（依赖 T1.11）
T3.6 MobileMcpBackend 实现（targetKind=physical）             技术选型 §9 [⏸️ BLOCKED: 需付费 Apple Developer 账号]
T3.7 AppiumDeviceBackend — physical: devicectl/WDA + WdaManager 技术选型 §9/ADR-011/ADR-012
     ✅ G5 7/7 PASS (iPhone 14 Plus, iOS 18.2.1)：ADR-012 WdaManager 自管 WDA 构建+安装，
     Route C (managed-xcodebuild + allowProvisioningDeviceRegistration) 在 2026-07-25 G5 中为推荐路径（历史结论；当前路线门禁见 ADR-028），
     Appium 管理 xcodebuild 启动含 provision 更新，旧 workaround (usePrebuiltWDA + xcodebuild -xctestrun) 降级为参考
T3.8 AgentRuntime：AI SDK 多步 tool-calling 包装              US-17.1/ADR-010
T3.9 ToolDispatcher：ToolCall→Zod→Permission→Backend→AgentEvent ADR-010
T3.10 AppiumDeviceBackend — simulator adapter: simctl/WDA      技术选型 §9/ADR-011（依赖 T3.7）
T3.11 BuildDriver XCUITest 路径(xcodebuild test → xcresult)      US-7.1
T3.12 DeviceBackend 探索执行 + run steps（physical+simulator）    US-8.1
T3.13 交互式录制：Agent 建议 + 用户确认/纠偏                    US-8.2
T3.14 断言策略(分层, 无断言不判 passed)                          US-11.1
T3.15 Flow 固化：录制→可重放 iTestAgent Flow YAML                US-9.1/9.2（依赖 T3.13）
T3.16 测试数据(生成/ TUI 询问)+ Keychain                         US-10.1/10.2
T3.17 Phase 3 集成测试：真机+Simulator 双目标联调验证
```

里程碑 M3：

```
- 有测试项目走 XCUITest；无测试走 DeviceBackend 探索
- 探索过程可固化为可重放 Flow
- 编排循环 + 权限引擎 + ToolDispatcher + ContextBuilder 工作；高风险操作 ask（ADR-010）
- 断言分层，未定不判 passed
- backend 支持可插拔：Appium/WDA 优先（免费账号可用，Route C G5 已验证：managed-xcodebuild + allowProvisioningDeviceRegistration），mobile-mcp 强候选（需付费账号），iphone-use 视觉后备
出口标准：US-6.x/7.1/8.x/9.x/10.x/11.1/17.x 的 P0 全绿（探索可靠性以诚实降级验收）
```

风险与缓解：签名/backend setup 走 doctor 引导；探索不稳退录制；子进程严格回收。

## 6. Phase 4：证据 / 性能 / 报告（约 W14-W16）

目标：稳健性能子集（physical+simulator 分域 baseline）+ 证据采集 + 三件套报告 + 失败归因。

任务：

```
T4.1 证据采集(截图/视频/syslog/crashlog/xcresult/trace)   US-13.1（physical+simulator 双来源）
T4.2 xcresult 解析(xcresultparser/xcparse)                US-7.1/数据流 S7
T4.3 PerformanceBackend: launch/memory/crash/duration/hitches  US-12.1（分域采集）
T4.4 PerformanceBackend: xctrace export(--toc/--xpath)+版本容错 数据流 S7/避坑第6（含 simulator trace）
T4.5 PerformanceBackend: FPS 近似(标 approximate)              US-12.1（simulator 标注来自Mac宿主）
T4.6 本地 baseline 建立与趋势对比                          US-12.2（physical/simulator 分域隔离）
T4.7 失败归因(证据驱动, 不足则 inconclusive)               US-14.1（含 targetKind 上下文）
T4.8 报告三件套(summary.md/result.json/artifact-index.json) US-15.1（simulator 报告含 environment 元数据）
T4.9 Phase 4 集成测试：证据/性能/报告联调验证（双目标验证）
```

里程碑 M4：

```
- 失败自动采证（physical+simulator）；性能主指标可采集并入 baseline（分域）
- FPS 标近似；xctrace summary 深解析为实验性(保留原始 .trace)
- 产出三件套报告（simulator 报告含 environment 元数据）并可解释失败
出口标准：US-12.1/12.3/13.1/14.1/15.1 P0 全绿；US-12.2 P1 允许限制；xctrace 深解析实验性
```

## 7. Phase 5：打磨与 MVP 验收（约 W17-W18）

目标：补齐重跑/延续/安全，CI 建立（含 macOS Simulator lane），对齐 19 条 MVP 完成标准（双目标），达可交付。

任务：

```
T5.1 质量门禁 G1-G7+G5-SIM 落地到 CI                      AI Native 第8（含 macOS Simulator CI lane）
T5.2 Flow 重放 run flow                                   US-9.2（同 targetKind 重放）
T5.3 测试代码 draft(实验性, 生成到 drafts/, 不自动入库)    US-20.1
T5.4 explain/rerun --failed-only                          US-14.1/16.1（targetKind 感知）
T5.5 安全与脱敏收尾(日志/报告/Keychain/写项目确认)         US-18.3/避坑第10（含 Simulator erase 高风险）
T5.6 MVP 验收对照(19 条，双目标)                           规格书 MVP 总表
```

里程碑 M5（MVP）：

```
- 19 条 MVP 完成标准：P0 全绿；P1 声明限制；实验性标注
- G5（真机）与 G5-SIM（Simulator）均通过验证
- 端到端可用：一句话 -> 真机或 Simulator 测试 -> 证据 -> 报告 -> 解释 -> 重跑
出口标准：具备第一版可交付条件（双目标）
```

## 8. Phase 6：真机全闭环收口（P0 恢复线，约 W19-W26）

目标：修复“组件和 Spike 已存在、生产入口未贯通”的实现偏移，交付可从 TUI 一句话触发、经用户确认后在真实 iPhone 完成测试并产出可解释、可重跑结果的产品闭环。

本阶段是 Phase 5 MVP 出口的纠偏主线，任务编号为 T6.1-T6.13。原可选增强任务迁移至 Phase 7（T7.1-T7.7）；在 M6-PHY 通过前，不再以组件单测、固定成功 probe、独立 backend spike 或手工构造报告输入作为“全闭环完成”证据。T6.12 保留既定双路径真机验收，后续 T6.13 补齐签名准备与修复向导并承担增量验证和最终阶段出口复核；完成 T6.12 不再单独关闭 Phase 6。

### 8.1 已锁定的实现偏移

```
- TUI AgentSession 仍注册 MockDeviceBackend，analyzeProject/getDeviceInfo 为提示或固定返回
- CLI explore 仅固定 launch + screenshot，未由已确认 TestPlan 驱动动态动作
- 真机 explore 只写 artifact-index.json 到临时目录，未接 RunStore/三件套报告
- run flow 默认只校验并依赖 --execute；执行分支使用错误 workspace 包名/构造方式，且会静默切换 mock dry-run
- rerun 只读取原 run/plan 并打印，尚未重新调度执行
- ReportSynthesizer、FailureExplainer、FlowReplay、BuildDriver 等组件存在，但缺少同一生产 run 的组合入口
- 部分所谓 MVP 集成测试只验证函数/薄桩存在，不能证明真实数据贯穿
```

### 8.2 任务拆解

```
T6.1  闭环契约与失败基线：锁定生产调用图、AC 矩阵、禁止 mock/薄桩冒充 E2E 的红测
T6.2  TUI 生产会话组合：真实 ProjectAnalyzer（含 analysisTier/limitations）/设备发现/BackendSelector/PermissionEngine，移除固定结果
T6.3  一句话到已确认计划：workspace -> Project Profile -> Intent -> TestPlan -> TUI 修改/确认/取消
T6.4  真机执行前置链：App 来源归一化/验证 -> xcodebuild -> R7 -> devicectl install/launch -> Route B/C 主动 readiness 对比与自愈（独占 DEF-031）
T6.5  双路径生产调度：确认前 metadata-only 解析 XCUITest 候选并锁定路径；仅权威确认无候选或显式选择时走 Appium DeviceBackend，indeterminate fail-closed，执行后禁止语义 fallback
T6.6  探索执行与 Flow 记录：按 ADR-031 区分自动探索/交互录制，动态动作、逐 case checkpoint、断言确认、canonical run steps、确认后固化 Flow
T6.7  Flow 生产重放：按 ADR-033 使 run flow 默认真实执行，锁定项目/全局 Flow 查找优先级，显式解析 targetKind/device，从 steps 推导 canonical requiredCapabilities，通过 BackendSelector 构造生产 backend，禁止 mock/dry-run fallback，保留 caseId 并输出可关联 step/evidence 事实
T6.8  RunStore 与报告贯通：依赖 T6.6 的 case/step/evidence 契约；按 ADR-034 使用单一 per-run writer 写入 ~/.itestagent，生成 plan.yaml + steps.json + 三件套 + artifacts，以 result.json 作为完整 bundle commit marker，并记录 evidence collection outcomes 与 legacy limitation
T6.9  解释与真实重跑：explain 消费本次 run 证据；rerun --failed-only 以权威 Target/Class/Method 精确调度 XCUITest 并关联 parentRunId；DeviceBackend 探索 case 阻断并引导 confirmed Flow 重放
T6.10 可靠性与安全收口：OpenTUI 交互、session teardown、T6.4 readiness 门禁回归、R7/R6/abort/redaction（不重复实现 DEF-031）
T6.11 自动化跨包闭环测试：同一生产组合分别覆盖 DeviceBackend 与 XCUITest 的 TUI 指令到 Run bundle/解释/重跑语义；test doubles 仅作 CI 外部传输替身
T6.12 G5 真机双路径验收：真实 iPhone 跑通两条执行路径并留档当前证据，人工确认后完成本任务，继续 T6.13
T6.13 签名准备与修复向导：首次 Team 配置、受控复用、失效恢复、生产 TUI/doctor 贯通与阶段出口复核
```

#### T6.11 自动化验收范围

T6.11 不是“把 MVP 19 项塞进一个测试文件”的发布总验收，而是 Phase 6 生产组合的跨包行为测试。必须覆盖：

```text
Lane A — DeviceBackend
TUI 生产会话 -> 项目/设备发现 -> Intent/TestPlan -> 确认 -> DeviceBackend 路由与 readiness -> 动态 case 动作/checkpoint -> Run Steps/Flow/证据 -> canonical Run bundle -> explain
自动探索 case 的 rerun --failed-only 必须在设备访问和 child run 创建前返回 rerun_case_not_reproducible，并引导 confirmed Flow 重放

Lane B — XCUITest
TUI 生产会话 -> metadata-only 候选与路径确认 -> XCUITest readiness -> xcodebuild test/xcresult parse -> canonical Run bundle -> explain -> 精确 failed-only child run(parentRunId)

Cross-cutting
同一生产 composition root、真实 PermissionEngine/RunStore/RunWriter/ReportSynthesizer/FailureExplainer/rerun compiler、runId 与 parentRunId 连贯性、R6/R7、AbortSignal、owner cleanup 与结构化失败语义
```

CI 允许替换设备、Appium、xcodebuild、LLM、时钟等外部传输或非确定性边界，但不得替换生产编排、权限、存储、报告、解释或重跑逻辑；测试必须断言跨包输入输出与落盘事实，不能只断言函数、import 或固定成功 probe 存在。doctor 命令、本地 Server 生命周期、真实 Simulator 构建安装、性能/baseline、安装与配置管理、测试草稿由既有专项测试和 G5/G5-SIM 证据继续验收，不属于 T6.11 单文件范围。

#### T6.12 验收中的 TUI 交付补齐

2026-09-07 经用户确认，当前验收修复包含启动欢迎区的自适应 `iTestAgent` 字符 Logo，以及在本次已保存 canonical 报告最终状态为 `passed` 时、报告目录之前显示绿色 `SUCCESS`。字标样式按后续用户确认统一为紧凑块状字符：启动字标淡青色，T/A 比小写主体高一行、基线一致，移除大字标下重复的小号名称；SUCCESS 使用同风格绿色大字标。紧凑终端使用单行名称/成功提示，须保留输入、报告与状态区域，任务开始后收起欢迎 Logo；错误、取消、未知或非 passed 不得显示成功。对应 US-4.1 AC7、US-15.1 AC8/AC9，使用纯逻辑和真实 renderer/PTY 回归验证，不改变原双路径 G5 范围，也不提前实现 T6.13。

T6.12 追加已批准的成功标识布局调整：完成提示 → 绿色四行块状 SUCCESS → 报告/证据路径；OpenTUI 大字标上下留白，按实际消息视口尺寸判断，不再按累计历史行数降为小字。保留窄屏/矮屏单行模式和原生滚动边界，补充顺序、长聊天、长报告路径、缩放及非 passed 回归。Ink/ANSI 同步显示顺序，保留其非滚动布局的空间预算。

T6.12 追加已批准的 US-2.3 设备选择修复：physical/simulator 同页分组与数量提示、跨类型一次性确认、精确目标重查、保留原目标和断言的计划重编译，以及刷新竞争/拒绝/设备失效/终态回归。ADR-038 记录规划边界；此项不自动 boot、重签或安装，不代替受影响执行链路的 G5/G5-SIM，也不提前实施 T6.13。

T6.12 追加已批准的 US-4.1 AC8 列表键盘统一：四类选择列表支持 ↑/↓ 与 Enter，候选保留 Space 多选、断言保留逐项采纳/拒绝、Esc 取消/返回；编辑时保留光标键。三种 renderer 共用事件语义，长列表选中项保持可见，跨类型与高风险确认不得由重复 Enter 绕过。通过单元、原生字符帧和真实 PTY 回归验证，不改变设备执行能力或 T6.13 排期。

T6.12 追加已批准的 US-4.2 AC5 长聊天布局修复：OpenTUI 消息区独立滚动与裁剪、底部跟随/手动翻阅保持、输入框与状态栏固定、SUCCESS/长报告路径不越界；补充原生字符帧的流式增长、鼠标/翻页键、中文换行与 resize 回归。仅属 TUI 验收缺陷修复，不改变 T6.13 或真机执行范围。

T6.12 PR #81 审查修复补充：断言敏感值阻断、多 case 条件归属失败关闭、空白 goal 与权限失败事件契约、权限事件异常 Promise 收束、API Key 特殊键过滤、AUT 准备阶段取消传播与 owned child 升级终止、WDA 身份缺失分类，以及 PTY/frame helper 异常清理和完整断言。自动化与真实 PTY 证据写入 `docs/06-verification/pr81-review-remediation-6.12.md`；DEF-034 当时保留真实设备取消验证门禁，不用替身测试关闭，不转移至 T6.13。

2026-09-08 DEF-034 独立授权复验：真实 iPhone 目标的 AUT build、build-settings、codesign 校验取消均经生产 executor 返回并落盘 cancelled；观察到的 owned 进程树无残留，已填充 staging 清理完成且延迟复查未重建，无安装/启动/WDA 操作。该延期项已 resolved，保留原历史与范围限制，证据见 `docs/06-verification/g5-def034-physical-cancellation-6.12.md`。用户已确认此前 G5 验收成功；本次不重复宣称 TUI/权限或 Simulator 验收，不单凭此专项验证推进 T6.12/T6.13 或 Phase 7，最终交付仍待单独确认。

T6.12 收尾归档（2026-09-08）：用户再次确认此前真实 iPhone 流程成功。本次只读复核五份 `physical / device_backend / passed` canonical bundle、3/3 步骤及各两份截图的路径/大小/SHA-256，全部通过，不重复真机测试。总报告为 `docs/06-verification/g5-physical-closed-loop-report-6.12.md`，分别列出用户确认、当前 run 和历史 XCUITest/Flow/rerun 专项证据；尚缺当前 T6.12 双路径证据的完整关联，不将历史结果冒充新执行。PR #82 尚未合并，任务保持 in_progress 待最终交付确认。DEF-035 全库扫描误报保持 open，须独立修复/复核，不否定已完成 DeviceBackend 流程，也不静默移除阶段门禁。

T6.12 性能缺口补充（2026-09-08，用户已批准）：上述真实 iPhone 成功仅证明 UI，未证明内存增长、泄漏或完整性能指标。先实现可注入采集生命周期、同一取消信号、异步有界导出、raw-local-only trace 落盘、逐指标状态与报告，再单独授权 G5/G5-SIM 性能验证；缺失指标不再返回健康的 false/0，UI 通过而请求指标缺失时整体 inconclusive。当前增量仅接入物理 DeviceBackend 的运行中 attach；冷启动、未知 schema、Simulator/XCUITest 采集、生产 baseline、多轮内存增长和泄漏诊断仍需后续专项接线/验证，不把本轮自动化通过当作全量性能完成。边界见 ADR-039，证据见 `docs/06-verification/performance-capture-wiring-6.12.md`。T6.12 保持 in_progress，T6.13 仍依赖其完成。

#### T6.13 签名准备与修复向导（待实施）

**登记日期**：2026-09-07。**类型**：代码任务，含增量真机验证与阶段出口复核。**依赖**：T6.12。**关联**：US-1.2/1.3、US-2.2/2.3、US-4.1、US-6.2、US-17.2、US-18.1/18.2。

目标体验：首次明确选择签名团队并完成真机准备，后续复用经验证的配置和产物；失效时由产品解释原因、引导用户完成必要操作，并在逐次授权后恢复。不是“签名永久有效”或“永久授权自动重签”。当前仅登记后续计划，不代表已有完整向导，也不授权本机账号、签名、设备或代码变更。

现有规格依据：US-1.3 要求“AC2 引导为分步骤、可复制命令 + 手机端操作说明”和“AC4 引导完成后可复检并显示已通过”；US-6.2 要求签名失败解释、主动 readiness 与高风险操作确认。实施范围：

1. **目标明确的诊断与 Team 选择**：在生产 TUI 与 doctor 中复用结构化诊断，检查 Xcode 账号可用性、所选 Team、Apple Development 签名身份、目标 WDA 的 bundle ID/profile/设备覆盖是否匹配；区分无账号、登录失效、无可用证书、团队不匹配、已证实的 profile 过期、网络失败与 WDA 启动失败。多团队时必须由用户选择，不以“本机有任一证书/profile”推断目标可签名；修正现有证书识别和非目标范围聚合造成的误判。
2. **可恢复的首次准备向导**：首次选择真机或遇到前置阻塞时进入分步骤页面，明确“正在检查/等待用户/正在执行/失败/完成”；提供可执行指引与复检入口。Apple 账号登录/验证码、手机信任与 Developer Mode 由用户完成；不索取或保存 Apple 密码，不替用户接受协议或绕过安全控制。Simulator 不强制进入真机签名向导，也不得静默切换目标类型。
3. **受控记忆配置与产物复用**：设计可查看、修改、撤销的本机签名选择；缓存必须绑定经确认的 Team、WDA bundle、设备与工具链版本，并在使用前验证兼容性和主动 readiness。不得硬编码任何个人账号/Team/设备/路径到仓库，不改变被测 App 或其他项目的签名配置。保存选择不等于保存高风险 allow，也不能把已安装当成签名仍有效。
4. **确认后的修复闭环**：通过现有 backend/WdaManager 和 Xcode 自动签名能力执行，不另造签名系统。修复前展示目标、影响与原因，重建/重签和覆盖安装分别经过 PermissionEngine 的单次授权；拒绝、超时或取消后不得执行后续操作。构建、签名、安装、启动和探测都有持续进度与取消边界；修复后重新验证目标 WDA `/status`、身份和 Appium session，再由用户恢复测试，不能仅凭构建成功宣称可用。
5. **失效与安全边界**：明确免费 Personal Team 的 profile 7 天有效期限制，允许复用配置但不承诺永久免维护；仅凭已安装清单或超时不得推断精确过期状态。账号重新认证、工具链变更、证书/profile 失效均须可解释、可复检；三 App 上限不得触发自动卸载，禁止全局杀死非本任务拥有的进程。

**实施前设计门禁**：ADR-028 当前规定“Team ID、UDID、账号和签名细节只在内存与脱敏诊断中使用，不写入报告或日志明文。”跨会话记住签名选择的最小元数据、存储位置/保护方式、授权与撤销、缓存失效规则，以及与既有 WDA 生命周期的职责衔接，必须先形成经确认的 ADR，并同步规格 AC、配置 schema 与数据流文档后再编码。本次登记不放宽 ADR-028/036，不把方案草案当成已批准的实现设计。

**计划验证与交付**：

- 自动化覆盖首次配置、重启复用、多 Team/证书/profile 不匹配、过期与非过期失败分类、重新认证/网络失败、每动作授权、拒绝/超时/取消、缓存失效、脱敏和 owner cleanup；复用 T6.11 生产组合回归，不用 helper 单测冒充用户入口闭环。
- 经单独授权，用真实 iPhone 从生产 TUI 验证“首次准备 -> 后续复用 -> 失效/不可用诊断 -> 确认修复 -> active readiness -> 测试恢复”，留存脱敏 G5 证据；不得为制造过期场景而改系统时间、撤销用户证书或删除用户数据。涉及 Simulator 共享路径变更时补 G5-SIM，否则明确不适用范围。
- 实施时新增对应测试并登记 `test_file`；计划报告为 `docs/06-verification/g5-signing-onboarding-report-6.13.md`（本次不创建或声称已验证）。完成后复核 Phase 6 全部出口和 open 延期项，包括 DEF-034 的真实处理状态，不因新增向导而转移或关闭 T6.12 的既有问题。
- T6.13 作为 Phase 6 最终出口复核任务；保留 T6.12 的原验收证据，受后续变更影响的链路必须复验，历史证据不替代本任务新增验证。代码、文档、自动化与所需 G5/G5-SIM 通过，并经人类合并 PR 后才可完成 T6.13；T6.12/T6.13 均完成且 M6-PHY 条件满足后才能进入 Phase 7。

### 8.3 依赖顺序

```
T6.1 -> T6.2/T6.3/T6.4
T6.2 + T6.3 + T6.4 -> T6.5
T6.5 -> T6.6
T6.6 -> T6.7 + T6.8
T6.7 + T6.8 -> T6.9
T6.2 + T6.4 + T6.6 + T6.9 -> T6.10
T6.6 + T6.7 + T6.8 + T6.9 + T6.10 -> T6.11 -> T6.12 -> T6.13 -> Phase 7
```

### 8.4 里程碑 M6-PHY（真机全闭环）

```
- 用户在 iOS workspace 运行 itestagent，从 TUI 输入一句测试目标
- Agent 使用真实项目分析与设备状态生成 TestPlan；未确认绝不执行
- 自动确定/构建/安装/启动 App，并完成 WDA/Appium 可用性检查；失败给出可执行修复建议
- T6.13 签名向导从生产入口支持首次 Team 配置、经验证的复用与逐次授权后的失效恢复；不要求用户重复研究底层签名命令，不承诺永久有效或免授权维护
- TestPlan 确认前按 ADR-029/030 metadata-only 解析 XCUITest 候选与探测三态；XCUITest 与 DeviceBackend 使用路径专属 readiness，工程构建与设备替换分别过权限门禁，执行开始后禁止静默跨路径 fallback
- 探索动作逐 case 采集 UI tree/截图并记录 run steps；Agent 建议断言必须经用户确认
- 同一 run 在 ~/.itestagent/runs/<run_id>/ 写入 plan.yaml、summary.md、result.json、artifact-index.json、artifacts/
- itestagent explain <run> 使用真实证据；itestagent rerun <run> --failed-only 对具有权威执行标识的 XCUITest case 真正执行并关联原 run；DeviceBackend 探索结果引导使用 confirmed Flow 重放
- 全链路无 MockDeviceBackend、固定成功 probe、错误包名或静默 dry-run fallback
- G1-G7 全过；真实 iPhone G5 新证据覆盖 exploration 路径与 XCUITest 路径（无测试资产时须提供专用 fixture app）
- T6.5 的 XCUITest 路径另需 CoreSimulator G5-SIM 证据；仅组件/fixture 测试不得宣称双目标闭环通过
- T6.12 双路径 G5 与 T6.13 增量验证/最终出口复核均完成
- DEF-025/029/030/031/032/033/034 逐条验证：已修复则标 resolved，未修复不得宣称 M6-PHY 通过；同时复核 Phase 6 全部 open 延期项，未修复的其他验收阻塞同样阻断出口
出口标准：一句话 -> 计划确认 -> 真机执行 -> Flow/证据 -> 三件套报告 -> 解释 -> 真实重跑，全程使用生产组合并由当前 G5 证据证明
```

## 9. Phase 7：增强路线（MVP 后，可选，M6-PHY 后执行）

```
T7.1 团队扩展(remote backend/Mac worker pool/共享设备池/PR Bot)  Optional
T7.2 xctrace summary 深解析(schema->归一化, 跨版本维护)   Deferred->正式
T7.3 断言增强(对话类流式完成断言原语等)                  提升探索可靠性
T7.4 核心链路推断增强(历史 Flow + 证据学习)              仍需人确认
T7.5 Flow 管理/导出 XCUITest/Appium 正式测试            从 draft 到可维护
T7.6 云扩展(BrowserStack/Sauce/AWS/Firebase 云真机矩阵)  Optional
T7.7 Phase 7 集成测试：增强路线验证                       Optional
约束：任何扩展不得破坏 Local-first、无登录、真机核心体验
```

## 10. 里程碑汇总

| 里程碑 | 阶段    | 关键出口                           | 相对周 |
| ------ | ------- | ---------------------------------- | ------ |
| M0     | Phase 0 | backend 横评 + 决策矩阵 + 路线确认 | W2     |
| M1     | Phase 1 | TUI/Server/Harness/doctor/devices 可用     | W5     |
| M2     | Phase 2 | Project Profile + 可确认 TestPlan  | W8     |
| M3     | Phase 3 | 双路径执行 + 可重放 Flow + 权限    | W13    |
| M4     | Phase 4 | 性能子集 + 证据 + 报告 + 归因      | W16    |
| M5     | Phase 5 | 19 条 MVP 达标，可交付             | W18    |
| M6-PHY | Phase 6 | 生产组合真机全闭环 + 签名向导/恢复 + 当前 G5 证据 | W26 基线；T6.13 增量待估 |
| M7     | Phase 7 | 可选增强路线，按需实施             | Optional |

## 11. 甘特图

## 12. 依赖关系

```
M0 -> 所有后续（真机链路不通则全部阻塞）
项目分析(P2) 依赖 结构解析可用 + 可选 index(依赖构建)
真机执行(P3) 依赖 M0 真机/Simulator 链路 + P1 doctor(1.11/b)/devices + P2 TestPlan
性能/报告(P4) 依赖 P3 执行产出证据（physical+simulator 双份）
MVP 验收(P5) 依赖 P1-P4 全部 P0（G5 + G5-SIM 双通过）
真机全闭环(P6) 依赖 P1-P5 组件，并以生产组合重新验证；T6.1 -> T6.2/3/4 -> T6.5 -> T6.6 -> T6.7/8 -> T6.9/10 -> T6.11 -> T6.12 -> T6.13
Phase 7 增强项(T7.1-T7.7)依赖 T6.13 完成（传递依赖 T6.12），不得挤占 M6-PHY 收口主线
断言可靠性/性能 baseline 有依赖链：指标质量 -> baseline -> 判定（分域隔离）
Simulator 能力依赖：T1.4(contracts) -> T1.5(simctl spike) -> T1.6(Appium/WDA spike) -> T1.12(diagnosis) -> T3.10(simulator adapter)
```

## 13. 单人独立开发者工作方式

单人 + AI Native 分工：人管决策，AI 管产出。人负责真机/Simulator spike 实测（G5 + G5-SIM）、签名与 backend setup 与设备处理、架构决策、AC 验收、危险操作确认、文档定稿；AI 负责读规格、出计划、写实现与测试、生成文档草稿。策略：串行推进；mock+fixtures 无真机也能写逻辑；真机/Simulator 任务集中验证；能复用绝不自研；研究级能力后置；每个 US 小步交付。

## 14. 工作量估算（相对）

| 阶段              | 规模                | 信心                 |
| ----------------- | ------------------- | -------------------- |
| Phase 0 多 Backend 横评  | S-M                 | 中（取决于真机环境） |
| Phase 1 骨架环境  | M                   | 高                   |
| Phase 2 分析+计划 | M-L                 | 中高（链路只做候选） |
| Phase 3 真机执行  | L-XL                | 中（探索式不确定 + Simulator 7-10 人周增量） |
| Phase 4 性能报告  | M-L                 | 中（xctrace 维护税 + Simulator 分域复杂度） |
| Phase 5 打磨验收  | M-L                 | 高（CI Simulator lane + G5-SIM 双门禁） |
| Phase 6 真机全闭环收口 | L                | 中（生产组合、签名/WDA、TUI 交互与真实重跑） |
| Phase 7 可选增强 | 按需                | 中低（研究级与外部服务依赖） |
| 合计到 MVP        | 约 28-36 周（单人） | 中（含 Simulator 同级支持 7-10 人周增量） |

注：上表规模为相对工量；单人全职 + AI Native 下约 26-32 周到 MVP（参考区间）。T6.13 为 2026-09-07 新增的后续修复，增量工期在实现计划确认时另估，不把原 W26 里程碑视为包含该增量的承诺。若坚持研究级能力（自动断言/FPS 精确/xctrace 深解析）作硬门槛，工期与风险显著上升，故列为增强。

## 15. 风险登记与应对

| 风险                | 阶段  | 影响       | 应对                                       |
| ------------------- | ----- | ---------- | ------------------------------------------ |
| 真机签名/backend 跑不通 | P0/P3 | 阻塞全局   | M0 先验证；doctor 引导；backend 预配置 |
| 无测试项目定位差  | P0/P3 | 探索失效   | 定位 spike；退纯录制；对话流式断言         |
| xctrace 跨版本变动  | P4    | 性能中断   | 薄封装+容错；主推 hitches；保留原始 .trace |
| AI 过度自信         | P2/P3 | 错误结论   | 候选+证据+确认；诚实降级                   |
| Simulator 性能误导    | P3/P4 | 错误结论   | baseline 分域隔离+报告标记；禁止跨域比较    |
| Appium/WDA Xcode 耦合 | P3    | Simulator失效 | 版本锁定；每次Xcode更新重跑 G5-SIM          |
| 可复现 vs 探索矛盾  | P3    | 结果不可信 | 固化 Flow 才复现；探索显式标注             |
| 范围蔓延            | 全程  | 延期       | 研究级后置；P0 门槛；backend 横评定路线       |
| 子进程泄漏          | P3/P4 | 稳定性     | server 统一生命周期与回收                  |
| 文档/绿测领先于生产组合 | P6 | 错误宣称可交付 | 出口测试必须从生产入口调用；禁止存在性测试/固定成功 probe 作为 E2E 证据 |
| 免费账号 WDA profile 失效 | P6 | 周期性真机失败 | T6.13 签名向导与受控复用/恢复；route-specific launch/status/session 主动探测 + R7 后重新签名，不能承诺一次签名永久有效，也不能只检查是否已安装或宣称无法证明的精确过期状态 |

## 16. 验收门槛（每阶段）

```
每阶段出口：对应 US 的 P0 全绿；P1 允许限制但需声明
真机相关：必须真机 spike 实测(G5)；Simulator 能力必须 CoreSimulator runtime 端到端验证(G5-SIM，ADR-011)。不接受“看代码就过”
产物：过 schema(plan/result/artifact-index/project-profile)
AI 产出：走 EPCC-V + G1-G7+G5-SIM
文档：有变更同步更新，避免规格漂移
研究级能力：以“尽力而为 + 诚实降级”验收，不以“全自动全对”验收
闭环证据：必须从用户可达的生产入口执行；独立 spike 只证明 backend 能力，不能替代产品闭环 G5
```

## 17. 交付物清单

```
可执行 itestagent CLI/TUI（本机安装）
本地 server + engine + analyzer + runner + backends + store
Project Profile / TestPlan / iTestAgent Flow 能力
性能子集采集 + 本地 baseline
三件套报告 + 失败解释 + 重跑
schemas/ + fixtures/ + mock backends
CI 门禁(G1-G7+G5-SIM) + 文档同步(含 ADR-011 Simulator 同级支持)
MVP 验收报告(对照 19 条)
M6-PHY 真机全闭环 G5 报告（生产入口、两条执行路径、报告/解释/重跑证据）
docs/05-planning/task-status.json（任务追踪）
docs/05-planning/deferred-items.json（延期待办追踪）
```

## 18. 一页速览

先多 Backend 横评定路线；骨架环境；先理解项目再计划；真机双路径与录制 Flow；稳健性能与证据与报告与归因；Phase 6 优先用生产组合收口“一句话 -> 计划确认 -> 真机执行 -> Flow/证据 -> 报告 -> 解释 -> 真实重跑”，当前 G5 证明后再做研究级增强；全程 AI Native（EPCC-V）、真机必实测、P0 门槛、文档不漂移。
