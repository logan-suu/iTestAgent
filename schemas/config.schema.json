{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://itestagent.dev/schemas/config.schema.json",
  "title": "iTestAgent Configuration",
  "description": "iTestAgent 配置文件 schema（对应 US-18.2 AC2：使用 JSONC，支持 $schema）",
  "type": "object",
  "properties": {
    "schemaVersion": {
      "type": "string",
      "description": "配置 schema 版本（AGENTS.md §5 数据契约：产物必须带 schemaVersion）",
      "default": "1.0"
    },
    "$schema": {
      "type": "string",
      "description": "JSON Schema 引用路径（US-18.2 AC2）"
    },
    "model": {
      "type": "object",
      "description": "模型配置（US-18.1 AC2：仅需本地依赖 + OpenAI-compatible 模型 API Key）",
      "properties": {
        "provider": {
          "type": "string",
          "description": "OpenAI-compatible provider 名称（如 openai、anthropic）",
          "default": "openai"
        },
        "baseURL": {
          "type": "string",
          "description": "自定义 API base URL（OpenAI-compatible 端点）"
        },
        "apiKeyRef": {
          "type": "string",
          "description": "API Key reference name (Keychain key or environment variable name). Plaintext API Key is never stored (R6 / US-18.2 AC3). Credential access is handled by task 1.10."
        },
        "model": {
          "type": "string",
          "description": "Model name (e.g., gpt-4o, claude-3-5-sonnet)"
        }
      },
      "additionalProperties": false
    },
    "device": {
      "type": "object",
      "description": "Device configuration (ADR-011: physical/simulator dual-domain).",
      "properties": {
        "preferredBackends": {
          "type": "object",
          "description": "Preferred DeviceBackend per target kind (ADR-011: targetKind-aware backend selection).",
          "properties": {
            "physical": {
              "type": "array",
              "items": { "type": "string", "enum": ["appium", "mobile-mcp", "mock"] },
              "description": "Backend preference order for physical devices.",
              "default": ["appium"]
            },
            "simulator": {
              "type": "array",
              "items": { "type": "string", "enum": ["appium", "mock"] },
              "description": "Backend preference order for simulator devices.",
              "default": ["appium"]
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "tui": {
      "type": "object",
      "description": "TUI 配置（技术选型 §5：OpenTUI 目标主线 / Ink 已验证 fallback）",
      "properties": {
        "framework": {
          "type": "string",
          "enum": ["opentui", "ink"],
          "description": "TUI 框架",
          "default": "opentui"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
