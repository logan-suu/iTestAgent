{
  "name": "itestagent",
  "private": true,
  "version": "0.0.1",
  "description": "iPhone 真机与 iOS Simulator 同级支持的全自动化测试 TUI Agent — Local-first, TUI-first, Agent-native, Project-aware, Target-explicit.",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*", "packages/itestagent-backends/*"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.base.json",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun test",
    "build": "echo \"ERROR: per-package build scripts not yet implemented (Phase 1). See task 1.10 for Phase 1 integration gate.\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "bun": "^1.3.14",
    "typescript": "^5.6.3",
    "webdriver": "9.29.1",
    "webdriverio": "9.29.1"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
