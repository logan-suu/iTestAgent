{
  "name": "itestagent",
  "private": true,
  "version": "0.0.1",
  "description": "iPhone 真机全自动化测试 TUI Agent — Local-first, TUI-first, Agent-native, Project-aware, Real-device only.",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*", "packages/itestagent-backends/*"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.base.json",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun test",
    "build": "echo \"TODO: per-package build scripts\" && exit 0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "bun": "^1.3.14",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
