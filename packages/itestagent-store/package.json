{
  "name": "itestagent-store",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "SQLite + Drizzle (metadata) + filesystem (artifacts)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4"
  },
  "peerDependencies": {
    "itestagent-contracts": "workspace:*"
  }
}
