{
  "name": "itestagent-backends-device-appium",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "DeviceBackend implementation — Appium/WDA (ADR-012: WdaManager owns WDA lifecycle, Appium handles WebDriver session only)",
  "dependencies": {
    "webdriverio": "9.29.1"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "bun test"
  }
}
