{
  "name": "itestagent-backends-analyzer-xcresult",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "xcresult parser adapter — wraps xcresultparser + xcparse CLI tools for normalized test results, metrics, and attachment extraction",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/"
  },
  "dependencies": {
    "itestagent-contracts": "workspace:*"
  }
}
