{
  "name": "itestagent-backends-build-xcodebuild",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "BuildDriver implementation — raw xcodebuild + xcbeautify (MVP default)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/"
  },
  "dependencies": {
    "itestagent-contracts": "workspace:*",
    "itestagent-backends-analyzer-xcodeproj": "workspace:*"
  }
}
