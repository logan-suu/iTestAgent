{
  "name": "itestagent-backends-performance-xctrace-analyzer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "PerformanceBackend implementation — xctrace analyzer core (MVP first candidate)",
  "dependencies": {
    "itestagent-contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "bun test"
  }
}
