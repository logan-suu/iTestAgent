{
  "name": "itestagent-process",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Subprocess/process-group ownership: spawn, TERM-to-KILL escalation, abort propagation, leader identity, reaping (zero internal workspace dependencies)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "bun test"
  },
  "dependencies": {}
}
